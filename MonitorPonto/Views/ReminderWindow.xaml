<Window x:Class="MonitorPonto.Views.ReminderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Monitor de Ponto"
        Width="500" Height="210"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent">

    <Border CornerRadius="12"
            Background="#EE1a1a1a"
            BorderBrush="#E94560"
            BorderThickness="2">

        <Grid Margin="24,18,24,18">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <TextBlock x:Name="TxtIcon"
                               FontFamily="Segoe UI Emoji" FontSize="30"
                               VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <TextBlock x:Name="TxtMensagem"
                               FontFamily="Segoe UI" FontSize="18" FontWeight="Bold"
                               Foreground="#E94560" VerticalAlignment="Center"
                               TextWrapping="Wrap"/>
                </StackPanel>

                <TextBlock x:Name="TxtHorario"
                           FontFamily="Segoe UI" FontSize="13"
                           Foreground="#AAAAAA" HorizontalAlignment="Center"/>

                <TextBlock x:Name="TxtProximo"
                           FontFamily="Segoe UI" FontSize="12"
                           Foreground="#F4A261" HorizontalAlignment="Center"
                           Margin="0,6,0,0"/>
            </StackPanel>

            <Button Grid.Row="1"
                    x:Name="BtnFechar"
                    Content="✕  Fechar"
                    FontFamily="Segoe UI" FontSize="13"
                    Background="#444444" Foreground="White"
                    BorderThickness="0" Padding="24,8"
                    Cursor="Hand" HorizontalAlignment="Center"
                    Margin="0,14,0,0"
                    Click="BtnFechar_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</Window>
